ffmpeg-python>=0.2.0
pathlib2>=2.3.7; python_version < "3.4"
SpeechRecognition>=3.10.0

# Optional — enable faster-whisper transcription:
#   pip install faster-whisper
#
# Optional — enable speaker diarization (faster-whisper only):
#   pip install pyannote.audio
#   IMPORTANT: pyannote requires speechbrain < 1.0 to avoid lazy-import crashes.
#   If you already have speechbrain >= 1.0, downgrade with:
#       pip install "speechbrain<1.0"
#   You must also:
#     1. Accept the license at https://huggingface.co/pyannote/speaker-diarization-3.1
#     2. Accept the license at https://huggingface.co/pyannote/segmentation-3.0
#     3. Set HF_TOKEN=your_token in your .env file
#
# Optional — enable Gemma 4 E2B-it local transcription:
#   pip install "transformers>=4.58" torch accelerate soundfile librosa
